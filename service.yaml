apiVersion: v1
kind: Service
metadata:
  name: spring-cashier-service 
  namespace: default
spec:
  type: NodePort
  externalTrafficPolicy: Local
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 32000
    protocol: TCP
  selector:
    name: spring-cashier
